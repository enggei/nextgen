delimiters "~","~"

gitignore(exclude) ::= <<~exclude:{it|/~it~/};separator="\n"~  >>

removeDir(dir) ::= <<git rm --cached -r ~dir~  >>

removeFile(path) ::= <<git rm --cached ~path~  >>

eom() ::= "}"

gt() ::= ">"
